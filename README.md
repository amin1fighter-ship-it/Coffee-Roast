# Coffee Roast Lab 0.4.15

Fix: removed duplicate `val live` in `showHome` that caused Kotlin **Conflicting declarations** and failed CI.

Upload **CoffeeRoast-v0.4.15-source.zip** to the repo root (you can delete the 0.4.14 zip).

Version `0.4.15` / versionCode `41500`.
