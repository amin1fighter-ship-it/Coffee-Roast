name: Build Android APK from Zip

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Extract and Align Project from Zip
        run: |
          ZIP_FILE=$(find . -maxdepth 1 -name "*.zip" | head -n 1)
          if [ -n "$ZIP_FILE" ]; then
            echo "Found zip file: $ZIP_FILE"
            mkdir -p unzipped_src
            unzip -q "$ZIP_FILE" -d unzipped_src
            
            # پیدا کردن هوشمند محل دقیق فایل gradlew داخل زیپ
            GRADLEW_PATH=$(find unzipped_src -name "gradlew" -type f | head -n 1)
            if [ -n "$GRADLEW_PATH" ]; then
              PROJECT_DIR=$(dirname "$GRADLEW_PATH")
              echo "Project root found at: $PROJECT_DIR"
              shopt -s dotglob
              cp -r "$PROJECT_DIR"/* ./
            else
              echo "Error: gradlew not found inside zip!"
              exit 1
            fi
            
            rm -rf unzipped_src
            rm -f "$ZIP_FILE"
          else
            echo "No zip file found."
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
